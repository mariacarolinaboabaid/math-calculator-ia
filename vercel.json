{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "ng build --configuration=production",
        "outputDirectory": "dist/math-calculator-ia"
      }
    }
  ]
}